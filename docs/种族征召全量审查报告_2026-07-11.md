# 种族征召全量审查报告

审查日期：2026-07-11

## 结论

图片资源已完成 P0 接入，未发现资源缺失、精确重复图片或空白映射。项目 UI 本轮未修改。

唯一需要 Gemini 补文案的项目是龙裔 `guild_barbarian` 职业试炼少 1 道题；该路线目前 6 道，其余开放路线均为 7 道。此项不影响图片资源接入，但会让该路线题量少于其他路线。

## 静态检查

| 检查项 | 结果 |
|---|---:|
| 开放种族 | 8 个 |
| 每族职业目标 | 4 个 |
| 每族体格题 | 5 道 |
| 每族结局模板 | 20 个，五类各 4 个 |
| 精确重复题干 | 0 |
| 图片清单槽位 | 66 |
| 图片清单就绪 | 66 |
| 资源文件缺失 | 0 |
| `node --check app.js` | 通过 |
| `node --check data/race-recruitment-assets.js` | 通过 |

题目选项的 `scoreDelta`、`mbtiDelta`、`alignmentDelta` 均可被现有加法逻辑累加；`expression` 是项目已有性格特征键，不是非法字段。

## 图片审查

本轮重新生成并替换 37 张：

- 32 张非巨魔种族职业目标图。
- 5 张通用公会结局图。

视觉复核标准：

- 横向 16:9 公会档案卡，不做个人 CG 或竖版半身像。
- 目标图至少保留种族锚点、职业工具和职业空间。
- 结局图能区分正式录取、试用、转岗、黑名单、荒诞录取。
- 中间调提亮、暗部可读、色彩分离明确，避免黑位糊成一片。
- 预览图统一接入 `*-preview.webp`，原图保留为 `*-source.webp`。

抽样复核覆盖人类、精灵、矮人、半身人、半兽人、提夫林、龙裔、食人魔/巨人八条路线；龙裔保留龙首/鳞片/血脉火光，食人魔/巨人保留体型尺度和环境参照，其他种族锚点均可辨识。图片预览像素比较未发现精确重复文件。

## 浏览器流程

内置浏览器使用 `http://127.0.0.1:4174/` 完成实际流程抽查：

- 首页和种族卡资源可请求；强制滚动懒加载资源后，首页 hero 为 `1920x1080`，种族卡为 `720x1080`。
- 人类战士路线完成 5 道体格题、7 道职业题、六维实战校准和 MBTI 八题。
- 结局页显示正式录取，结局图匹配 `guild_ending_accepted-preview.webp`。
- 角色卡显示 ISTJ、人类、战士，六维雷达、体质/魅力评分和职业贴合度均有数值。
- 页面文字显示“征召判断 60% / 六维实战 40%”，与评分实现一致。

## 后续

下一步只处理 UI 重写建议和龙裔狂战士缺失文案。本报告不要求继续生成 P1 的逐题场景图；当前 263 张 P1 题目图仍按既定规则回退到目标图或种族卡，属于未启用的低优先级图片需求。

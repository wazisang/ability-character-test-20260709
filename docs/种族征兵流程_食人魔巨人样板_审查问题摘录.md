# 种族征兵流程_食人魔巨人样板 审查问题摘录

源文件：

`C:\Users\henan\.gemini\antigravity\brain\baedff25-6473-4375-af84-e3d0ab4661dd\scratch\ogre_giant_questions.json`

## 结论

已处理并接入。

原始 `ogre_giant_questions.json` 不建议直接接入；本次已按项目字段结构生成修正版，并接入运行数据：

- 修正版文档：`docs/种族征兵流程_食人魔巨人样板_Gemini生成结果_修正版.json`
- 运行数据：`data/race-recruitment-ogre-giant.js`
- 页面加载：`index.html`
- 接入代码：`app.js`

当前接入路线采用：`guild_barbarian / guild_fighter / guild_cleric / guild_druid`，即保留 Gemini 文案里的“牧师 / 誓约医疗所”路线，并同步更新了项目首页预设和目标主题。

校验结果：

- `physiqueChecks`: 5 道
- `trialSets`: 4 条路线，每条 7 道
- `endingTemplates`: 20 条，五类结局各 4 条
- `mbtiFilters`: 4 组，均补齐 4 条职业 Flavor
- 浏览器实测：可从首页进入食人魔/巨人，选择誓约医疗所，并推进到 `誓约医疗所试炼 1/7`

以下内容保留为原始文件审查记录，供下次 Gemini 返修或生成时对照。

这份文件的题量是够的：

- `raceId`: `ogre_giant`
- `physiqueChecks`: 5 道
- `trialSets`: 4 条路线，每条 7 道
- `endingTemplates`: 20 条
- `mbtiFilters`: 4 组

但原始字段结构和当前项目不兼容，直接接入会导致目标卡显示缺字段、结局无法按类型筛选、推荐职业读不到。

## 原始文件阻塞问题

### 1. 征召路线与项目预设不一致

项目当前 `ogre_giant` 预设路线是：

- `guild_barbarian`
- `guild_fighter`
- `guild_druid`
- `guild_warlock`

本文件实际路线是：

- `guild_barbarian`
- `guild_fighter`
- `guild_cleric`
- `guild_druid`

问题：

- 缺 `guild_warlock`
- 多了 `guild_cleric`

建议：

- 若维持当前项目预设，请把 `guild_cleric` 整条路线改为 `guild_warlock`。
- 如果确实想让食人魔 / 巨人走牧师路线，则需要同步改项目首页路线预设和后续目标图需求。

### 2. `recruitTargets` 字段不完整

当前每个目标只有：

- `targetId`
- `name`
- `description`
- `requirements`

项目目标卡需要：

- `targetId`
- `classNameZh`
- `classNameEn`
- `title`
- `oneLine`
- `recruiter`
- `recruitPitch`
- `acceptedStyle`
- `likelyFailure`
- `hiddenRoute`
- `recommendedStats`
- `riskTags`

请让 Gemini 不要使用 `name / description / requirements` 作为最终字段，必须补齐上述字段。

### 3. 结局类型不符合项目

当前 20 条结局的类型分布：

- `accepted`: 12 条
- `rejected`: 6 条
- `special`: 2 条

项目需要：

- `accepted`: 4 条
- `probation`: 4 条
- `transferred`: 4 条
- `blacklisted`: 4 条
- `absurdAccepted`: 4 条

请把 `rejected` 改写为 `blacklisted` 或 `probation`，把 `special` 改写为 `absurdAccepted`，并补足 `transferred`。

### 4. `recommendedClass` 格式错误

当前结局里使用：

- `"Barbarian"`
- `"Fighter"`
- `"Cleric"`
- `"Druid"`
- `"None"`

项目需要使用目标 ID：

- `guild_barbarian`
- `guild_fighter`
- `guild_druid`
- `guild_warlock`

如果保留牧师路线，则使用 `guild_cleric`，但要同步改项目路线预设。

不要使用 `"None"`。拒绝类结局也要给出可演归宿，例如：

- `recommendedClass`: [`guild_barbarian`]
- 或 `recommendedClass`: [`guild_druid`]

### 5. `transferred` 结局缺 `rejectedClass`

项目的转岗结局需要：

```json
{
  "endingType": "transferred",
  "recommendedClass": ["guild_druid"],
  "rejectedClass": "guild_fighter"
}
```

当前没有可用的 `transferred` 结局。

### 6. `mbtiFilters` 不完整

当前 4 组 MBTI 滤镜存在，但每组只写了一个职业 Flavor。

项目需要每组都有 4 个职业 Flavor。若使用当前项目预设路线，则每组都要包含：

- `guild_barbarianFlavor`
- `guild_fighterFlavor`
- `guild_druidFlavor`
- `guild_warlockFlavor`

如果保留牧师路线，则把 `guild_warlockFlavor` 换成 `guild_clericFlavor`，但同样需要同步项目预设。

## 文风与安全建议

以下词或表达建议修改，避免从奇幻吐槽滑到现实羞辱或过度低俗：

- `智力赤字`
- `智商盆地`
- `战术白痴`
- `王八`
- `自残截肢`
- `胃穿孔`
- `贪吃致死`

替换方向：

- “判断失控”
- “荒野直觉过载”
- “战术理解粗暴”
- “高危事故档案”
- “被列入异常行为观察”
- “公会拒绝正式录用，但边境承包队对你的破坏力很感兴趣”

食人魔 / 巨人可以保留巨体、食量、笨拙、荒诞和破坏力，但不要把角色写成现实智力羞辱对象。

## Gemini 返修指令

请基于现有 `ogre_giant_questions.json` 局部返修，不要全量推翻题目。

必须修：

1. 路线统一为 `guild_barbarian / guild_fighter / guild_druid / guild_warlock`，或明确改项目预设后统一为 `guild_barbarian / guild_fighter / guild_cleric / guild_druid`。
2. `recruitTargets` 补齐项目字段，不要只用 `name / description / requirements`。
3. `endingTemplates` 改成五类各 4 条：`accepted / probation / transferred / blacklisted / absurdAccepted`。
4. `recommendedClass` 全部改成 `guild_*` ID，不要用英文职业名或 `None`。
5. `transferred` 结局必须补 `rejectedClass`。
6. `mbtiFilters` 每组补齐 4 个 `${targetId}Flavor`。
7. 删除或替换现实智力羞辱和过度低俗表达。

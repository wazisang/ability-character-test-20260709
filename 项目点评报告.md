# 能力角色仪 — 详细点评报告

> 评审对象：`ability-character-test-20260709`  
> 评审时间：2026-07-14  
> 代码规模：app.js 6318 行 / styles.css 5132 行 / 数据文件 ~51000 行总计

---

## 一、设计意图不明确

### 问题诊断

项目同时承载了**至少四套互不相同的产物体系**，且没有在入口处让用户理解自己将要经历什么：

| 子系统 | 做了什么 | 代码位置 |
|---|---|---|
| 六维能力检定 | 6 个属性轴 × 15 个互动小游戏（点击波、弹幕闪避、符文记忆…） | `ATTRIBUTES` (L1-77) + `renderTest` (L2618) |
| MBTI 人格测试 | 4 题快速推断 + 16 型人格滤镜 + 职业气质描述 | `renderMbtiProbe` (L4298) + `MBTI_ARCHETYPES` (L346) |
| 种族征召流程 | 13 个种族 × 4 条职业路线 × 3 幕叙事 × 3 场实战 | `FANTASY_ANCESTRIES` (L213) + `renderRecruitmentQuestionScreen` (L1881) |
| D&D 建卡 | 12 个职业池 × 副职业 × 阵营 × 缺陷卡 × 体质鉴定 | `DND_CLASS_POOL` (L720) + `buildSummary` (L5564) |

**入口页（`renderStart`）的核心矛盾**：

- 标题写的是「能力角色仪 | 冒险者检定与出卡」，但首屏文案立刻切入「公会征召大厅」「血脉选择」「志愿投递」——用户不知道这是一个能力测试、一个人格测试、还是一个跑团建卡器。
- 首屏同时展示了：英雄区插图 + 征召流程 6 步印章 + 24 个种族预览卡牌 + 当前预览详情面板 + 「今日可投」职业方向 + 「联动状态」说明 + 底部细则。**这些信息服务于不同心智模型**——有人想看种族卡牌，有人想理解流程，有人想直接开始——但它们平铺在同一个视口里，没有主次。
- `fineprint` 文案（L1436）用一整段话解释了「每种血脉四条志愿路线、三幕叙事、三场双轴实战、MBTI 扮演滤镜」——这实际上是在用文字补偿设计没说清楚的事。

### 改进建议

1. **确立单一核心叙事**：这个工具的核心价值是「测出你的六维属性 → 生成一张 D&D 角色卡」。所有其他系统（种族征召、MBTI、缺陷卡）应该作为这个核心的**修饰层**，而不是平行的入口。
2. **入口页只做一件事**：一个标题 + 一句话说明 + 一个「开始」按钮。把种族选择、征召流程等放在开始之后的步骤里，而不是首屏全摊开。
3. **用流程图代替文字说明**：当前用 `recruit-seals`（6 个 span）和 `fineprint` 文字来解释流程。可以用一个简单的 4 步视觉流程图（选种族 → 做检定 → 看结果 → 出卡）替代。

---

## 二、排版混乱

### 问题诊断

**入口页（renderStart）**：

```
┌─────────────────────────────────────────────┐
│ recruit-hero (两列)                          │
│  ┌─ copy: 插图 + eyebrow + h1 + lead +      │
│  │  seals + actions + fineprint              │
│  └─ notice-board: 24张种族卡 + 详情面板      │
├─────────────────────────────────────────────┤
│ recruit-home-grid (两列)                     │
│  ┌─ target-ledger: 「今日可投」4个方向卡     │
│  └─ linked: 「联动状态」标签                 │
└─────────────────────────────────────────────┘
```

问题在于：hero 区的 notice-board 里已经有 24 张种族卡 + 详情面板，下面的 home-grid 又重复展示了「今日可投」和「联动状态」——**信息层级没有拉开**，hero 区和 grid 区的视觉权重接近，用户不知道先看哪里。

**结果页（renderResult）**：

```
┌─ recruitment-result-panel (征召判定) ──────────┐
├─ result-hero (标题 + pill 标签) ────────────────┤
├─ result-grid (两列: 雷达图 + 立绘) ──────────────┤
├─ dnd-class-panel (职业线索板: 4列 × N张卡) ──────┤
├─ result-grid (两列: 属性清单 + 检定日志) ────────┤
├─ result-grid (两列: 主职业描述 + 描述词) ────────┤
├─ result-grid (两列: 体质鉴定 + 缺陷卡) ──────────┤
├─ result-panel (给DM的摘要: 30行textarea) ───────┤
└────────────────────────────────────────────────┘
```

结果页有 **7 个主要区块**，其中 4 个是两列网格——纵向滚动距离极长（估计 3000px+），所有面板的视觉权重几乎相同，没有「主角」和「配角」的区分。

**CSS 层面**：
- 只有 2 个断点（860px / 520px），中间尺寸（平板竖屏）缺乏适配
- `.dnd-match-grid` 固定 4 列（`repeat(4, minmax(0, 1fr))`），在窄屏下卡片会被压到极窄
- `.race-card-grid` 固定 3 列，24 张卡 = 8 行，在入口页占据大量空间

### 改进建议

1. **结果页改成分层披露**：第一屏只展示角色卡核心（雷达图 + 立绘 + 一句话定位 + TOP1 职业），其余内容用 Tab 或折叠面板组织（「详细属性」「职业线索」「体质与缺陷」「DM 摘要」）。
2. **入口页砍掉冗余区块**：notice-board 的 24 张种族卡和 home-grid 的「今日可投」「联动状态」存在信息重叠。建议入口页只保留：标题 + 插图 + 开始按钮 + 可选的种族快速选择（不超过 6 个）。
3. **增加中间断点**：在 768px 和 1024px 增加响应式断点，确保平板和中小笔记本上有合理的列数。
4. **视觉权重分层**：用尺寸、留白和背景色区分主次——核心内容用大面板 + 浅色背景，辅助信息用小卡片 + 透明背景。

---

## 三、职业选项过多

### 问题诊断

当前系统的「选择空间」构成：

| 维度 | 数量 | 来源 |
|---|---|---|
| 种族 | 13 个 | `FANTASY_ANCESTRIES` (L213-344) |
| 每种族征召路线 | 4 条 | `recruitTargets` (各 race-recruitment-*.js) |
| D&D 职业池 | 12 个 | `DND_CLASS_POOL` (L720-841) |
| 每职业副职业 | 3-4 个 | `baseSubclasses` 字段 |
| MBTI 类型 | 16 个 | `MBTI_TYPES` (L193-211) |
| MBTI 职业滤镜 | 16 个 | `MBTI_ARCHETYPES` (L346-364) |

**理论组合数**：13 × 4 × 12 × 4 × 16 = **39,936 种**角色卡变体。

**用户实际面对的选项过载**：

- **入口页**：24 张种族预览卡同时展示（`getHomePreviewRaces(24)`，L1405）
- **种族选择页**：13 个种族卡 + 每个都带插图 + 标签 + 描述（`renderRaceSelect`，L1690）
- **征召目标页**：4 个目标卡，每个都有插图 + 标题 + 招募文案 + 录取打法 + 常见失败 + 隐藏路线 + 风险标签（`renderRecruitTargetCard`，L1819）——单张卡片信息量就已经很大
- **结果页职业线索板**：`dnd-match-grid` 用 4 列网格展示多个 DnD 匹配卡（`renderDndMatchCard`，L4901），每张卡有 TOP 排名 + MBTI + 职业名 + 副职业 + 分数 + 匹配说明
- **结果页描述词**：5 组 × 6 个标签 = 30 个描述词同时展示

**核心矛盾**：系统试图让用户在 13 个种族 × 4 条路线中做选择，但这些选择对最终结果的影响是**模糊的**——用户无法预知选「巨魔·狂战士路线」和选「精灵·游侠路线」会导致什么样的结果差异。选择没有带来掌控感，反而带来焦虑。

### 改进建议

1. **种族选择收敛到 6 个以内**：将 13 个种族分为「推荐开放」和「即将上线」两组，首屏只展示 5-6 个有完整数据的种族，其余折叠或灰显。
2. **征召路线改为 2-3 条**：4 条路线 × 完整文案（录取打法 + 常见失败 + 隐藏路线 + 风险标签）信息过载。可以只展示路线名称 + 一句话描述，点击后展开详情。
3. **结果页职业线索只展示 TOP 3**：当前展示多张匹配卡。改为只突出 TOP 1（大卡片 + 立绘），TOP 2-3 用小条目展示，其余折叠。
4. **将「选择」改为「确认」**：系统已经根据六维属性和 MBTI 自动计算了最佳职业匹配，用户的种族/路线选择更多是「风味」而非「机制」。可以弱化选择的重量感——让用户选种族时知道「这只影响角色的故事风味，不影响能力评分」。

---

## 四、交互体验差

### 问题诊断

**流程过长且不可回溯**：

完整流程的步骤数：

```
入口页 → 种族选择 → 征召目标 → 体格鉴定(3题) → 职业试炼(3题) 
→ 复合校准(3场小游戏) → 公会判定 → 个人档案 → 六维检定(15场小游戏) 
→ MBTI推断(4题) → 扮演姿态(4题) → 结果页
```

共计 **30+ 个交互节点**。但：

- **无进度持久化**：`state` 对象是纯内存的，刷新页面 = 全部丢失。30 步流程中任何一步刷新都需要从头来。
- **回退能力有限**：大部分页面只有一个「返回」按钮，没有面包屑或步骤导航。`renderTest` 中的检定流程（L2618）完全没有回退——一旦开始检定，只能逐个完成或跳过。
- **全量重渲染**：每个步骤切换都执行 `app.innerHTML = ...`（如 L2626, L4472, L4755），没有过渡动画，没有局部更新。屏幕切换是硬切，体验生硬。

**小游戏交互不一致**：

- 有的用 Canvas（`renderDodge` L3338, `renderDualBall` L4001）
- 有的用 DOM 元素（`renderClickWaves` L2859, `renderSchulte` L4144）
- 有的用表单选择（`renderAbstractQuiz` L3757, `renderCharismaQuiz` L4211）
- 反馈方式各不相同：有的有 `showArenaFeedback`（L2844），有的没有
- 「交给 DM 掷默认骰」按钮（L2638）给固定 5.2 分——这是一个惩罚性的跳过机制，不是真正的「DM 代掷」

**结果页交互问题**：

- 30 行的 `textarea`（L4871）作为「DM 摘要」——用户需要上下滚动才能看完，且无法分段复制
- 「复制给 DM」按钮直接复制整段文本，没有格式化或分段选项
- 雷达图用 Canvas 绘制（`drawRadar()`），但 Canvas 不可交互——无法 hover 查看具体数值
- 职业线索板的卡片没有点击交互——不能查看职业详情

### 改进建议

1. **增加进度持久化**：将 `state` 序列化到 `sessionStorage`，每次 `render()` 后自动保存。刷新页面时检测并恢复。这是 30 步流程的**必须项**。
2. **添加步骤导航**：在顶部 progress bar 下方增加可点击的步骤面包屑（种族 → 检定 → 结果），允许用户跳回到之前的步骤修改选择。
3. **屏幕切换加过渡**：用 CSS `opacity` + `transform` 做简单的淡入效果，比 `innerHTML` 硬切好很多。
4. **统一小游戏交互模式**：所有小游戏统一使用「说明 → 开始 → 进行中 → 结算」四阶段模式，统一反馈方式（如统一的 `showArenaFeedback`）。
5. **结果页改成分段展示**：用 Tab 切换（「角色卡」「属性详情」「职业线索」「DM 摘要」），避免一页滚动 3000px。
6. **DM 摘要改为结构化卡片**：把 30 行 textarea 拆成多个可单独复制的小卡片（属性、职业、阵营、缺陷等），或者提供「复制为 Markdown」选项。
7. **雷达图改为可交互**：hover 属性轴时高亮并显示具体数值，或者改用 SVG 实现（天然支持交互）。

---

## 总结

| 维度 | 严重程度 | 核心问题 |
|---|---|---|
| 设计意图 | ★★★★ | 四套系统平行展示，入口无法传达核心价值 |
| 排版 | ★★★☆ | 入口页和结果页信息过载，视觉层级未拉开 |
| 职业选项 | ★★★☆ | 13 种族 × 4 路线 × 12 职业的组合空间远超用户决策能力 |
| 交互体验 | ★★★★ | 30+ 步流程无持久化、无回退、全量重渲染、小游戏不统一 |

**一句话总结**：这个项目的**内容丰富度是惊人的**——种族文案、小游戏种类、职业匹配算法、缺陷卡系统——但它的**信息架构和交互流程没有跟上内容的复杂度**。它需要做的不是加东西，而是**做减法**：减入口信息、减结果页平铺、减选择数量、减流程步数，让已有的丰富内容以更克制的方式呈现。

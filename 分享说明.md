# 能力角色仪使用说明

这是一个可离线打开的网页小工具。

## 最简单打开方式

双击 `index.html`，用浏览器打开即可。

## 如果浏览器直接打开异常

如果图片或脚本没有正常加载，可以用本地服务运行：

1. 安装 Node.js。
2. 在这个文件夹里打开命令行。
3. 执行：

```bash
node server.js
```

4. 浏览器打开：

```text
http://127.0.0.1:4174/
```

## 说明

- `assets/` 是图片资源。
- `data/` 是测试、种族征召和卡牌数据。
- 不需要安装依赖，也不需要联网。
